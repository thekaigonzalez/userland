// BEGIN USERLAND KERNEL SOURCE AUTOGENERATED CODE
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

#define i64 long long
#define u64 unsigned long long
#define i32 int
#define u32 unsigned int
#define i16 short
#define byte char

#define UVersion char*
#define UError char*
#define UStr char*

#define Malloc(bytes) malloc(bytes)
#define Realloc(bytes) realloc(bytes)
#define Free(bytes) free(bytes)
#define CAlloc(bytes) calloc(bytes)

extern int Print (UStr);
extern char* Concatenate (char*, char*);

// END USERLAND KERNEL SOURCE AUTOGENERATED CODE

#include <stdarg.h>
i32 main() {
// this file contains a information about the system

UVersion __version = "0.0.1"; // version of the system

printf("Version: %s\n", __version);
Print("welcome to userland");

char* n = Malloc(11);

n[0] = 'h';
n[1] = 'e';
n[2] = 'l';
n[3] = 'l';
n[4] = 'o';
n[5] = ' ';
n[6] = 'w';
n[7] = 'o';
n[8] = 'r';
n[9] = 'l';
n[10] = 'd';
n[11] = '\0';

Print(n);

Free(n);

// Print(Concatenate("hello ", "world"));

}

